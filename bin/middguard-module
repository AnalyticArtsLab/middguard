#!/usr/bin/env node

let program = require('commander');

program
  .version(require('../package.json').version)
  .command('analytics', 'Create an analytics module')
  .command('vis', 'Create a visualization module')
  .parse(process.argv);
