script(type="text/template", id="graph-editor-template").
  <div class="header">
    <h4><%= name %></h4>
  </div>
  <div class="sidebar">
    <ul class="modules-list"></ul>
  </div>

script(type="text/template", id="graph-node-template").
  <circle class="outline" r="<%= r %>" cx="<%= r %>" cy="<%= r %>"></circle>
  <text x="<%= r %>" y="<%= r %>" style="text-anchor: middle;">
    <%= displayName %>
  </text>
  <circle class="drag-handle"
    cx="<%= handlePosition.x %>" cy="<%= handlePosition.y %>" r="20"></circle>
  <path
    class="drag-handle"
    transform="translate(<%= handlePosition.x %>,<%= handlePosition.y %>)"
    d="<%= dragHandlePath %>"></path>
  <circle class="status status-<%= status %>"
    cx="<%= statusPosition.x %>" cy="<%= statusPosition.y %>" r="7"></circle>
  <% inputs.forEach(function(input, i) { %>
    <circle class="connector input" data-name="<%= input.name %>"
      cx="<% print(inputPosition(i, r, inputs.length).x) %>"
      cy="<% print(inputPosition(i, r, inputs.length).y) %>"
      r="5"></circle>
  <% }); %>
  <% if (output) { %>
    <circle class="connector output" r="5" cx="<%= r %>" cy="<%= 2 * r - 10 %>">
  <% } %>
