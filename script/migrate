#!/usr/bin/env node

var program = require('commander');

program
  .version('0.0.1')
  .command('make', 'Make a new migration file')
  .command('latest', 'Update database to latest migration')
  .parse(process.argv);